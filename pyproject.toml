[tool.poetry]
name = "homework6"
version = "0.1.0"
description = "SQLlite_db Univer"
authors = ["BGU"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
